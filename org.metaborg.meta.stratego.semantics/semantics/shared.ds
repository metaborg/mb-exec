module shared

imports
  base
  aterm
  stratego-signatures
  
signature
  
  arrows
    Term -default-> Value
    Term -defs-> D
    
  // success and failure values
  values
    S : T
    F :
  
  semantic-components
    E -> Map<String, T>
    D -> Map<Term, Term>

signature
  native-operators
    length : List(Term) -> Int
