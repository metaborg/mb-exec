module shared

imports
  base
  aterm
  stratego-signatures
  
signature
  
  arrows
    Term -default-> Value
    
  // success and failure values
  values
    S : T
    F :
  
  semantic-components
    E -> Map<String, T>
    
signature
  native-operators
    length : List(Term) -> Int
    concatStrings : String * String -> String
