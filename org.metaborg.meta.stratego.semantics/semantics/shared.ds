module shared

imports
  base
  aterm

signature
  
  arrows
    Term -default-> Value
    Term -defs-> D
    
  // success and failure values
  values
    S : T
    F :
  
  semantic-components
    E -> Map
    D -> Map

signature
  native-operators
    length : List(Term) -> Int
