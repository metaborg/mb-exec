module shared

imports
  aterm
  stratego-signatures

signature

  arrows
    Term --defs> D
    Term --default> Value
    
  // success and failure values
  values
    S : T
    F :
  
  semantic-components
    E -> Map
    D -> Map

signature
  native-operators
    length : List(Term) -> Int
  
