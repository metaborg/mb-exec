module test2
imports
  lib
  EclipseJava
  ecj-adapter

strategies
  
  main = where(
      !FILE(<id>) 
    ; debug 
    ; ecj-parse-only
    ; topdown(try(?TypeDeclaration(_, _, _, _, _, _) ; debug))
    ) ; !()
